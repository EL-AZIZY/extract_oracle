# requirements.txt
# Dépendances Python pour extract_pg (prod-ready)

# Oracle driver (si source Oracle)
oracledb>=1.4

# PostgreSQL driver (source/target PostgreSQL + audit SQL)
psycopg2-binary>=2.9

# MySQL driver (si source MySQL)
mysql-connector-python>=8.0

# Parquet support
pyarrow>=14.0

# Logging JSON optionnel (si activé plus tard)
python-json-logger>=2.0

# Resource monitoring
psutil>=5.9
